{
  "name": "pocketmux-native",
  "private": true,
  "version": "0.1.0",
  "description": "Pocketmux native connection shell for Windows and Android",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/connection.js && node --check src/connection-initialization.js && node --check src/connection-navigation.js && node --check src/connection-operations.js && node --check src/connection-switch.js && node --check src/connection-validation.js && node --check src/connection-authentication.js && node --check src/credential-migration.js && node --check src/drawer-gesture.js && node --check src/error-state.js && node --check src/floating-handle.js && node --check src/i18n.js && node --check src/native-shell-session.js && node --check src/rejected-credential.js && node --check src/remote-viewport.js && node --check src/storage.js && node --check src/remote-session.js && node --check src/validated-credential.js && node --check src/main.js",
    "test": "node --test",
    "dev": "tauri dev",
    "build": "tauri build",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:icons": "tauri icon src-tauri/icon-source/icon-manifest.json -o src-tauri/icons && node scripts/prepare-android-icon-layers.js && node scripts/sync-android-icons.js",
    "android:build": "npm run android:icons && tauri android build",
    "windows:build": "tauri build --bundles nsis,msi",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
